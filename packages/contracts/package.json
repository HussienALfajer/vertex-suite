{
  "name": "@vertex/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The shared vocabulary: identifiers that cross module boundaries and the grammar of a permission. Knows nothing about any module.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vertex/kernel": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "vitest": "4.1.11"
  }
}
