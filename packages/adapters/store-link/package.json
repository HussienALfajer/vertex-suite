{
  "name": "@vertex/store-link",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The wire between a register and its store node: the paths and answers of operation delivery, read by both ends from here.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@vertex/platform": "workspace:*"
  },
  "devDependencies": {
    "@vertex/kernel": "workspace:*",
    "typescript": "6.0.2",
    "vitest": "4.1.11"
  }
}
